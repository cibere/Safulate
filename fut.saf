type TypeName {
    # edit type body
};

type TypeName[key, val]{
    # edit type body
};
pub x = TypeName[a, b];
x.key == a; x.val == b;

type TypeName(other){
    # compare type and instance
};

type TypeName{} -> (
    # init args
){
    # init body
};